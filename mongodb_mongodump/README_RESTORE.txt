MongoDB BSON dump of database odisha_vision_2047 (mongodump 2026-04-05).

Restore (from this folder):
  mongorestore --uri="mongodb://localhost:27017" "./odisha_vision_2047"

JSON seed files are still in database/ (mongoimport as in README).
